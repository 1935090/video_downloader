import { StyleSheet } from 'react-native'
import { Colors, Fonts, Metrics, ApplicationStyles } from '../../Themes/'

export default StyleSheet.create({
  footer: {
    elevation: 0,
    height: 4.5 * Metrics.baseMargin,
    backgroundColor: '#0F1724',
    borderTopWidth: 0
  }
})
